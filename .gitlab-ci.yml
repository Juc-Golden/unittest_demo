stages:
- build
- test
- deploy

build_maven:
  stage: build
  script:
   - echo "build unittest....."
   - echo "done"
  only:
   - master
  tags:
   - gitlab-runner_unittest 


deploy_springboot:
  stage: deploy
  script:
   - echo "deploy unittest...."
   - chmod -R 777 /home/deploy_project/unittest_demo
   - /home/deploy_project/gitlab_deploy/deploy_unittest.sh
   - chmod -R 755 /home/deploy_project/unittest_demo
   - echo "done"
  only:
   - master
  tags:
   - gitlab-runner_unittest  
